from math import sqrt

m = float(input('Enter mean and varience'))
var = float(input())
m = float(m)
var = float(var)
print(f'{m} - sqrt({var}) = {m - sqrt(var)}')
print(f'{m} + sqrt({var}) = {m + sqrt(var)}')