from math import sqrt
import numpy as np

# m = float(input('Enter mean and varience'))
# var = float(input())
# m = float(m)
# var = float(var)
# print(f'{m} - sqrt({var}) = {m - sqrt(var)}')
# print(f'{m} + sqrt({var}) = {m + sqrt(var)}')

l = np.array(range(10))
print(l[l > 5])